name: Deploy Lambda Function

# Trigger the workflow on push to the main branch
on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check out the repository
    - name: Checkout source code
      uses: actions/checkout@v3

    # Step 2: Set up AWS CLI
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AKIATCKAQPJUUPHZLM24 }}
        aws-secret-access-key: ${{ secrets.LWM9uLXMnEPFE3/SUibhBc/iLOQRx56Dd2KwKBfQ }}
        aws-region: ${{ secrets.us-west-2 }}

    # Step 3: Install dependencies (if needed, e.g., for Lambda layers or libraries)
    - name: Install dependencies
      run: |
        pip install -r requirements.txt  # If you have dependencies listed in requirements.txt
        zip -r lambda_function.zip .      # Zip the Lambda code

    # Step 4: Update Lambda function code
    - name: Deploy Lambda Function
      run: |
        aws lambda update-function-code \
          --function-name ${{ secrets.lambdacicd }} \
          --zip-file fileb://lambda_function.zip

    # Optional: Verify the deployment
    - name: Verify Lambda Deployment
      run: |
        aws lambda get-function-configuration --function-name ${{ secrets.lambdacicd }}
